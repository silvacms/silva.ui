(function($) {
  /**
   * SMIMain properties screen
   *
   * Shows a form for changing properties of a folder, publication, document or file.
   */
  $.fn.SMIProperties = function() {
    var $SMIProperties = this;

    $SMIProperties.html('<div><input type="text" value="" /></div>');
  };
})(jQuery);