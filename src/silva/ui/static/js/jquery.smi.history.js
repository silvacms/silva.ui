(function($) {
  /**
   * SMIMain history screen
   *
   * Shows a table of file/document revisions and publishing options.
   */
  $.fn.SMIHistory = function() {
    var $SMIHistory = this;

    $SMIHistory.html('<div>History here</div>');
  };
})(jQuery);