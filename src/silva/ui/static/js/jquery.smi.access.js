(function($) {
  /**
   * SMIMain access screen
   *
   * Shows a form for changing access rights for users/groups
   */
  $.fn.SMIAccess = function() {
    var $SMIAccess = this;

    $SMIAccess.html('<div>Access here</div>');
  };
})(jQuery);